# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(usv_camera_simulationTaskLib)
ADD_LIBRARY(${USV_CAMERA_SIMULATION_TASKLIB_NAME} SHARED
    ${USV_CAMERA_SIMULATION_TASKLIB_SOURCES})
add_dependencies(${USV_CAMERA_SIMULATION_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${USV_CAMERA_SIMULATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${USV_CAMERA_SIMULATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${USV_CAMERA_SIMULATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${USV_CAMERA_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${USV_CAMERA_SIMULATION_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${USV_CAMERA_SIMULATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${USV_CAMERA_SIMULATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${USV_CAMERA_SIMULATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/usv_camera_simulation)

